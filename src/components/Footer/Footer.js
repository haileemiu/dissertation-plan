import React from 'react';
import './Footer.css'

// Function Component (preferred if don't have state)
// This is essentially just the render part. With an automatic return.
const Footer = () => (
  <footer>
    &copy; Prime Digital Academy
  </footer>
);

export default Footer;
